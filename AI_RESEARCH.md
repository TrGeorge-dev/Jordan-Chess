# AI V3 算法选择记录

本分支以公平的成对换色对战选择算法。每组使用同一开局进行两盘，双方交换黑白，避免把先手优势误判为 AI 提升。

## 被保留的主线

V3 使用可撤销并查集维护连通分量，并以增量方式推导立即胜点和后续威胁。在此状态上运行迭代加深 PVS（Principal Variation Search），配合置换表、历史启发、杀手走法和有限战术延伸。

V3 与修改前默认 AI 的验证结果：

- 0.12 秒/步，8×8 与 10×10 共 40 局：V3 8 胜、28 和、4 负，得分率 55.0%。
- 0.4 秒/步，10×10 共 8 局：V3 4 胜、2 和、2 负，得分率 62.5%。

## 被否决的策略

### 蒙特卡洛树搜索

在相同状态表示、威胁判定、时间和开局下，MCTS 与 PVS 对战 24 局：MCTS 1 胜、1 和、22 负，得分率 6.25%。当前算力下，随机模拟无法替代本游戏所需的精确战术计算，因此实现未进入正式代码。

### 独立证明式威胁搜索

证明器能够严格找到连续强制胜法，但作为 PVS 前置层时，24 局消融实验为 5 胜、13 和、6 负，得分率 47.9%。它消耗了普通搜索时间，却没有提高总体胜率，因此未进入默认算法。

### 自动评价权重变异

小棋盘训练选出的候选权重在独立的 8×8/10×10 验证中取得 8 胜、7 和、9 负，得分率 47.9%，存在过拟合，已否决。

## 结论

当前证据支持继续发展增量状态上的 PVS，而不是叠加多个搜索器或手工修补局面。任何新策略都必须先作为独立实现通过换色对战，胜出后才能进入 Python/HTML 默认 AI。
